# Base URL of the OpenTripPlanner routing service, no trailing slash.
# Unset defaults to http://localhost:8080, which is where routing/README.md tells you
# to run it locally. A deployed build must point at a hosted instance — a static
# Netlify build cannot reach your laptop.
VITE_OTP_BASE_URL=http://localhost:8080
